node_modules/
docs/spikes/results/
.expo/
dist/
web-build/


# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.env.local

# Reporte de QA autocontenido (qa-harness/build-report.py --embed).
# El que se versiona es docs/qa/<pase>/index.html.
QA-REPORT.html

# Clave de servicio de Google Play para `eas submit` (#93). Nunca commitear.
play-service-account.json

# Clave de App Store Connect API para `eas submit` iOS. Nunca commitear.
AuthKey_*.p8
asc-api-key.json
.secrets/

# Salida de `expo prebuild` (CNG). Se regenera con `npx expo prebuild`.
/ios
/android

# Worktrees de agentes
.worktrees/
