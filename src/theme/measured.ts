/**
 * Prototype values not yet in design/tokens.json.
 * Track C does not edit the frozen token files.
 */
export const measured = {
  // settings `clearFg` — "Borrar mi historial" (design HTML ~1212)
  danger: "#B0603F",
} as const;
